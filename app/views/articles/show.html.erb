<div class="col-10 col-mx-auto">
  <div class="card">
    <div class="card-header">
      <div class="card-title h5"><%= @article.title %></div>
      <div class="card-subtitle text-gray"><%= "Topic" %></div>
    </div>
    <div class="card-body">
      <%= raw @article.content %>
    </div>
  </div>
</div>
